# Render Postgres — use the External Database URL for Vercel / local
# Dashboard → your Postgres → Connect → External Database URL
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DATABASE

# Optional: force SSL for non-Render hosts
# PGSSL=true
