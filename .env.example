# Postgres for Vercel / local (Neon or Render External URL)
# Neon: dashboard → Connection string (include sslmode=require)
# Render: Dashboard → Postgres → Connect → External Database URL
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DATABASE?sslmode=require

# Optional: force SSL for other hosts
# PGSSL=true
